import {SubPageBannerContainer} from './styles'

export const SubPageBanner = ({children}) => {
    return (
        <SubPageBannerContainer>
            {children}
        </SubPageBannerContainer>
    );
};
