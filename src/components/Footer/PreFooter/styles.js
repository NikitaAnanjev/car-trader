import styled from '@emotion/styled'

import {Flex} from "@chakra-ui/react"

export const PreFooterContainer = styled(Flex)`
  position: relative;
  width: 100%;
  flex-wrap: wrap;
  background: url("./bg/prefooter-banner.jpg") no-repeat center right;
  background-size: cover;
  min-height: 30vh;
  justify-content: center;
  align-items: center;
`
